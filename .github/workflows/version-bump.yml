name: Bump Version

on: push

jobs:
  patch-version:
    name: 'Bump Patch Version'
    if: "contains(github.event.ref, 'PATCH')"
    runs-on: ubuntu-latest

    steps:
      - name: 'Checkout source code'
        uses: 'actions/checkout@v3'
        with:
          ref: ${{ github.ref }}
      - name: 'cat package.json'
        run: cat ./package.json
      - name: Logging
        run: |
          echo "${{toJSON(github.event.ref)}}"

